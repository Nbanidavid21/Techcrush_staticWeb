<<<<<<< HEAD
name: CI/CD Website Deployment
=======
name: Deploy Static Webpage to AWS EC2
>>>>>>> a4854505ef7200c332e5fabd98880129fc9bfdee

on:
  push:
    branches:
      - main  # Triggers the pipeline on push to the main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Sync files to S3
      run: aws s3 sync . s3://my-ci-cd-website-bucket --delete

    - name: Invalidate CloudFront cache (optional)
      run: aws cloudfront create-invalidation --distribution-id ABC123XYZ --paths "/*"
